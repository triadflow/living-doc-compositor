# Advisory Profile Policy

The default AI advisory profile sets must remain document-grounded unless a later ticket explicitly opts into tool-backed context. This source uses different wording from query text so retrieval has to bridge policy, advisory, and profile terms rather than only exact phrases.

Verification note: derived retrieval is only a hydration hint. Canonical living-doc JSON and GitHub tickets must be checked before status changes.
