interface:
  display_name: "Inference Unit Chain Extension"
  short_description: "Add inference-unit types or policies with chain handoff tests."
  default_prompt: "Use this skill when adding a new inference-unit type or routing policy to preserve controller-owned handoff, artifact refs, tests, and generated report evidence."
